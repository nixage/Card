import { ShowBackCardDirective } from './show-back-card.directive';

describe('ShowBackCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowBackCardDirective();
    expect(directive).toBeTruthy();
  });
});
