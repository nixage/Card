import { ShowFrontCardDirective } from './show-front-card.directive';

describe('ShowFrontCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowFrontCardDirective();
    expect(directive).toBeTruthy();
  });
});
